#ifndef TEXTUREHOLDER_HPP
#define TEXTUREHOLDER_HPP
#include <vector>
#include <iterator>
#include "SFML\Graphics.hpp"

class TextureHolder
{
public:
    TextureHolder();
	
	sf::Texture kelp;
	sf::Texture ramachandra;
	sf::Texture tMainMenu;
	sf::Texture tPlayButton;
	sf::Texture placeHolder;
};

#endif //TEXTUREHOLDER_HPP
